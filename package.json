{
  "name": "react-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "concurrently \"npm run build --prefix client\" \"npm run build --prefix server\"",
    "install-deps": "npm install --prefix client && npm install --prefix server",
    "start": "concurrently \"npm run start --prefix client -- PORT=3001\" \"npm run start --prefix server -- PORT=3000\""
  },
  "dependencies": {
    "concurrently": "^3.5.1",
    "debug": "^4.1.1",
    "express": "^4.16.4",
    "http-errors": "^1.7.1",
    "mongoose": "^5.4.11",
    "morgan": "^1.9.1",
    "nodemon": "^1.18.10",
    "pug": "^2.0.3"
  }
}
